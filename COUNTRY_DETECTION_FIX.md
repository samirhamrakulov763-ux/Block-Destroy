# ✅ Исправлено определение страны пользователя

## Дата: 2026-04-20

---

## 🐛 Проблема:

**Было:**
- Код страны определялся по **языку телефона**
- Если язык русский → флаг России (даже если пользователь в Узбекистане)
- Если язык английский → флаг США (даже если пользователь в другой стране)

**Результат:**
- Неправильные флаги в рейтинге
- Пользователи видели не свою страну

---

## ✅ Решение:

### 1. Создан новый класс `CountryDetector.cs`
- Определяет страну по **IP адресу** (реальное местоположение)
- Использует бесплатный API: `http://ip-api.com/json/`
- Запасной вариант: если нет интернета → определяет по языку системы

### 2. Обновлён `GameManager.cs`
- При запуске игры определяет страну по IP
- Сохраняет код страны в `GameData.CountryCode`
- Определение происходит только один раз (при первом запуске)

### 3. Обновлён `GameData.cs`
- Убрано значение по умолчанию "us"
- Теперь система всегда определяет страну заново

---

## 🌍 Как работает:

### При первом запуске игры:
1. Игра запускается
2. `GameManager` отправляет запрос к IP API
3. API возвращает код страны (например: "uz" для Узбекистана)
4. Код сохраняется в `GameData.CountryCode`
5. Этот код используется в рейтинге

### Поддерживаемые страны:
- 🇺🇿 Узбекистан (uz)
- 🇷🇺 Россия (ru)
- 🇰🇷 Корея (kr)
- 🇺🇸 США (us)
- 🇨🇳 Китай (cn)
- 🇯🇵 Япония (jp)
- 🇹🇭 Таиланд (th)
- 🇻🇳 Вьетнам (vn)
- 🇮🇩 Индонезия (id)
- 🇸🇦 Саудовская Аравия (sa)
- 🇪🇸 Испания (es)
- 🇵🇹 Португалия (pt)
- 🇫🇷 Франция (fr)
- 🇩🇪 Германия (de)
- 🇮🇹 Италия (it)
- 🇹🇷 Турция (tr)
- И другие...

---

## 🔧 Технические детали:

### API запрос:
```
GET http://ip-api.com/json/?fields=countryCode
```

### Ответ:
```json
{
  "countryCode": "UZ"
}
```

### Таймаут:
- 5 секунд
- Если API не отвечает → используется язык системы

### Кэширование:
- Код страны сохраняется в `PlayerPrefs`
- Определение происходит только один раз
- При переустановке игры определяется заново

---

## 🧪 Тестирование:

### Как проверить:
1. Удалить игру с телефона (чтобы сбросить PlayerPrefs)
2. Установить заново
3. Запустить игру
4. Проверить Console: должно быть `"Country detected by IP: uz"` (или другой код)
5. Открыть рейтинг → флаг должен соответствовать реальной стране

### Проверка без интернета:
1. Отключить интернет
2. Запустить игру
3. Должен определиться код по языку системы
4. В Console: `"Failed to detect country by IP, using system language"`

---

## 📋 Созданные файлы:

- `Assets/Block Destroy/Scripts/Common/_Manager/CountryDetector.cs` - новый класс
- `Assets/Block Destroy/Scripts/Common/_Manager/CountryDetector.cs.meta` - мета-файл Unity

## 📝 Изменённые файлы:

- `GameManager.cs` - добавлено определение страны по IP
- `GameData.cs` - убрано значение по умолчанию "us"

---

## ✅ Результат:

Теперь в рейтинге будут показываться **правильные флаги** в зависимости от реального местоположения пользователя!

- Пользователь в Узбекистане → флаг 🇺🇿
- Пользователь в России → флаг 🇷🇺
- Пользователь в Корее → флаг 🇰🇷
- И так далее...

---

**Готово к тестированию!** 🚀
